[profile.default]
src = "src"
out = "out"
libs = ["lib"]
evm_version = "cancun"
optimizer = true
optimizer_runs = 200

# Map @openzeppelin imports → lib/openzeppelin-contracts
remappings = [
    "@openzeppelin/=lib/openzeppelin-contracts/"
]

[rpc_endpoints]
rayls_devnet = "https://devnet-rpc.rayls.com"